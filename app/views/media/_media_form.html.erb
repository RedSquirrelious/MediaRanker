<%= form_for @my_medium, 
method: @post_method, 
url: @post_path do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :maker %>
	<%= f.text_field :maker %>

	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.submit %>
<% end %>